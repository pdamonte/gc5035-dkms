PACKAGE_NAME="gc5035-dkms"
PACKAGE_VERSION="0.1.1"

BUILT_MODULE_NAME[0]="gti5035"
BUILT_MODULE_LOCATION[0]="."
DEST_MODULE_LOCATION[0]="/updates/dkms"

AUTOINSTALL="yes"
MAKE[0]="make KERNEL_RELEASE=${kernelver} KERNEL_BUILD=/lib/modules/${kernelver}/build"
CLEAN="make clean KERNEL_RELEASE=${kernelver} KERNEL_BUILD=/lib/modules/${kernelver}/build"
